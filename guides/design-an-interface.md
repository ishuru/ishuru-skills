# design-an-interface — Guide

Generate multiple radically different interface designs using parallel sub-agents.

## Usage

```
Design an interface for [module]
Design it twice for [API/component]
```

## Approach

Spawns parallel sub-agents, each producing a distinct interface shape. Compare and select the best.
