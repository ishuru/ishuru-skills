# cli-creator — Guide

Scaffold complete Node.js CLI tools from a description.

## Usage

```
Create a CLI for [purpose]
Generate a CLI tool that [does X]
```

## Output

- `package.json` with commander setup
- CLI entry point with subcommands
- Config management
- Formatted output (table, JSON, plain)
- Test scaffolding
